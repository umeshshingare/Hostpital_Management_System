# Legacy Console Application

This folder contains the original command-line Hospital Management System (plain Java + JDBC).

The active application is now the full-stack web project in `backend/` and `frontend/`.
